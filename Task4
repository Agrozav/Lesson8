import datetime
now = datetime.datetime.now()

year = lambda x:x.year
month = lambda x: x.month
day = lambda x: x.day
t = lambda x: x.time()
print('Время сейчас ',t(now),'\nСегодняшнее число',day(now),month(now),year(now))
